import { requireAdmin } from "@/server/auth.js";
import { readState } from "@/server/store.js";
/* eslint-disable @typescript-eslint/no-explicit-any */
export const runtime = "nodejs";
export async function GET(request: Request) { const state = await readState(); const denied = requireAdmin(state, request); if (denied) return denied; const meals = Object.values(state.userData).reduce((count: number, data: any) => count + Number(data.today?.meals?.length || 0) + (data.history || []).reduce((sum: number, day: any) => sum + Number(day.meals?.length || 0), 0), 0); const month = new Date().toISOString().slice(0, 7); const usage = state.aiUsage.filter((item) => item.month === month); const byFeature = usage.reduce((result, item) => { const key = item.feature || "other"; result[key] = (result[key] || 0) + Number(item.cost || 0); return result; }, {}); return Response.json({ application: "ok", database: "ok", storage: "ok", ai: state.ai.encryptedKey ? "configured" : "attention", users: state.users.length, activeUsers: state.users.filter((user) => !user.disabled).length, meals, aiRequests: usage.length, estimatedAiCost: usage.reduce((sum, item) => sum + Number(item.cost || 0), 0), byFeature }); }
